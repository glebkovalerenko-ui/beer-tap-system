# Протокол тестирования: Hardware Integration Layer (v1.0.0)

## **Раздел 1: Введение и мета-информация**

### **1.1. Цель документа**

Настоящий документ определяет исчерпывающий набор тестовых сценариев для проверки корректности работы слоя интеграции с оборудованием (`Hardware Integration Layer`) проекта **Beer Tap System**. Фокус данного протокола — тестирование взаимодействия программного обеспечения с **реальным физическим оборудованием** в штатных и нештатных ситуациях. Цель — гарантировать надежность, отказоустойчивость и предсказуемость поведения системы на самом низком, "железном" уровне.

### **1.2. Объекты тестирования**

1.  **`RPi Controller` (Контроллер на Raspberry Pi)**
    *   **Ответственность:** Автономное управление логикой налива, фиксация транзакций в локальной SQLite базе данных, фоновая синхронизация с центральным сервером, реакция на глобальные системные команды (например, Emergency Stop).
2.  **`NFC Handler` (Обработчик NFC в Admin App)**
    *   **Ответственность:** Обеспечение надежного моста между USB NFC-считывателем (`ACR122`) и пользовательским интерфейсом. Работает по событийно-ориентированной модели, проактивно информируя UI о появлении/исчезновении карты или ошибках оборудования.

### **1.3. Предусловия и тестовый стенд**

Для выполнения данного протокола тестирования необходим следующий стенд:

*   **Оборудование:**
    *   1 x **Raspberry Pi 4 Model B** (или новее) с установленной и настроенной последней версией ПО `rpi-controller`.
    *   1 x **ПК (Windows/macOS/Linux)** с установленным `Admin App` последней сборки.
    *   1 x **USB NFC-считыватель `ACR122U`**, подключенный к ПК с `Admin App`.
    *   1 x Набор (минимум 3 шт.) **RFID-карт `MIFARE Classic 1K`**.

*   **Программное обеспечение:**
    *   Полностью развёрнутый и работающий `Backend API` последней версии (запущенный через `docker-compose` в той же локальной сети).
    *   База данных PostgreSQL должна быть в состоянии, соответствующем последней миграции Alembic.

*   **Инструменты:**
    *   SSH-клиент для доступа к командной строке Raspberry Pi.
    *   Клиент для просмотра SQLite БД на RPi (например, `sqlite3` в командной строке).
    *   Доступ к инструментам разработчика в `Admin App` для просмотра системных логов и событий.
    *   Доступ к логам `rpi-controller` (вывод в консоли или файл).

### **1.4. Легенда и руководство по заполнению**

| Элемент | Описание |
| :--- | :--- |
| **ID** | Уникальный идентификатор тест-кейса. |
| **Приоритет** | `P1` (Критический), `P2` (Высокий), `P3` (Средний). Определяет очередность выполнения. |
| **Статус** | `[ ]` - To Do, `[P]` - Pass, `[F]` - Fail, `[B]` - Blocked. Заполняется инженером в ходе тестирования. |
| **Сценарий** | Описание теста в формате Gherkin. |
| **Комментарий / Bug ID** | Заполняется в случае статуса `Fail` или `Blocked` с указанием номера задачи в баг-трекере. |

### **1.5. **Визуализация тест-плана: Hardware Integration Layer**

```mermaid
mindmap
  root((Тест-план: Hardware Integration Layer))
    ::icon(fa fa-clipboard-check)
    (Модуль 1: RPi Controller Logic)
      ::icon(fa fa-raspberry-pi)
      (Фоновый поток синхронизации)
        TC-HW-RPI-01 (P1): Успешная синхронизация после офлайна ✅
        TC-HW-RPI-02 (P2): Сервер отклоняет транзакцию ❌
      (Обработка режима "Emergency Stop")
        TC-HW-RPI-03 (P1): Блокировка наливов при активном режиме ЧС 🛑
        TC-HW-RPI-04 (P1): Возобновление работы после отключения ЧС ✅
      (Отказоустойчивость (физическое взаимодействие))
        TC-HW-RPI-05 (P2): Карта убрана во время налива 🚫
        TC-HW-RPI-06 (P3): Истек таймаут ожидания ввода ⏳
      (Отказоустойчивость (сбои сети / Fail-Safe))
        TC-HW-RPI-07 (P1): Блокировка при невозможности проверить статус ЧС 🔌
    (Модуль 2: NFC Handler (Tauri/Rust))
      ::icon(fa fa-id-card)
      (Поток событий и обнаружение карты)
        TC-HW-NFC-01 (P1): Успешное считывание UID ✅
        TC-HW-NFC-02 (P2): Корректное состояние "карта убрана" 💨
      (Обработка ошибок оборудования)
        TC-HW-NFC-03 (P1): Отключение/подключение USB-считывателя 🔌
        TC-HW-NFC-04 (P2): Открытие UI при отключенном считывателе ❌
        TC-HW-NFC-05 (P3): Стресс-тест: быстрое прикладывание карты ⚡️
      (Интеграция с бизнес-логикой)
        TC-HW-NFC-06 (P1): Обработка внешней ошибки от сервера (карта занята) ‼️
```
---

## **Раздел 2: Протокол тестирования**

### **Модуль 1: RPi Controller Logic**

| ID | Приоритет | Статус | Сценарий (Gherkin) | Комментарий / Bug ID |
| :--- | :--- | :--- | :--- | :--- |
| **TC-HW-RPI-01** | P1 | `[ ]` | **Scenario:** Успешная синхронизация наливов после работы в офлайн-режиме<br>**Given** RPi-контроллер запущен и подключен к сети<br>**And** Локальная БД SQLite пуста (таблица 'pours')<br>**And** Сервер 'Beer Tap System' доступен<br>**When** На RPi выполняется команда `sudo ifconfig eth0 down` для отключения сети<br>**And** Пользователь эмулирует 3 успешных налива подряд<br>**Then** В локальной БД SQLite в таблице 'pours' должны появиться 3 новые записи со статусом "new"<br>**And** В логах контроллера должны присутствовать сообщения об ошибках синхронизации (e.g., "Failed to connect to server")<br>**When** На RPi выполняется команда `sudo ifconfig eth0 up` для восстановления сети<br>**Then** В течение следующего цикла синхронизации (15-20 секунд) все 3 транзакции должны быть успешно отправлены на сервер<br>**And** Статус всех 3 записей в таблице 'pours' должен измениться на "confirmed" | |
| **TC-HW-RPI-02** | P2 | `[ ]` | **Scenario:** Негативный сценарий: Сервер отклоняет транзакцию<br>**Given** RPi-контроллер запущен и подключен к сети<br>**And** Сервер настроен так, чтобы отклонять следующий налив от известной карты (e.g. UID '11223344')<br>**When** Пользователь эмулирует 1 налив с картой UID '11223344'<br>**Then** В локальной БД SQLite в таблице 'pours' должна появиться 1 новая запись со статусом "new"<br>**And** В течение следующего цикла синхронизации транзакция должна быть отправлена на сервер<br>**And** Сервер должен вернуть ответ со статусом "rejected" для этой транзакции<br>**Then** Статус записи в таблице 'pours' должен измениться на "failed"<br>**And** В логах контроллера должна появиться строка с причиной отклонения (e.g., "Transaction rejected by server. Reason: Insufficient funds") | |
| **TC-HW-RPI-03** | P1 | `[ ]` | **Scenario:** Контроллер блокирует наливы при активном режиме "Emergency Stop"<br>**Given** RPi-контроллер запущен и подключен к сети<br>**And** Сервер 'Beer Tap System' доступен<br>**And** Глобальный флаг "Emergency Stop" на сервере установлен в 'true'<br>**When** Пользователь подносит валидную RFID-карту к считывателю<br>**Then** В логах контроллера должна появиться строка "OPERATION BLOCKED BY SERVER (EMERGENCY STOP)"<br>**And** Контроллер не должен переходить к эмуляции налива<br>**And** Новая запись в таблице 'pours' в локальной БД не должна быть создана | |
| **TC-HW-RPI-04** | P1 | `[ ]` | **Scenario:** Контроллер возобновляет работу после отключения "Emergency Stop"<br>**Given** RPi-контроллер запущен, и наливы заблокированы из-за активного режима "Emergency Stop"<br>**When** Глобальный флаг "Emergency Stop" на сервере изменяется на 'false'<br>**And** Пользователь снова подносит валидную RFID-карту к считывателю<br>**Then** Контроллер должен предложить пользователю начать налив<br>**And** После эмуляции налива в таблице 'pours' должна появиться новая запись со статусом "new" | |
| **TC-HW-RPI-05** | P2 | `[ ]` | **Scenario:** Пользователь убирает карту во время "эмуляции налива"<br>**Given** RPi-контроллер запущен и готов к наливу<br>**When** Пользователь подносит карту и нажимает 'Enter' для начала эмуляции налива<br>**And** В середине процесса "налива" пользователь убирает карту от считывателя<br>**Then** В логах контроллера должна появиться строка "Card removed. Operation cancelled."<br>**And** Транзакция не должна быть сохранена в локальную БД SQLite | |
| **TC-HW-RPI-06** | P3 | `[ ]` | **Scenario:** Пользователь не успевает нажать 'Enter' за отведенное время<br>**Given** RPi-контроллер запущен, пользователь поднес карту<br>**And** Контроллер предложил нажать 'Enter' для начала налива<br>**When** Пользователь не предпринимает никаких действий в течение 10 секунд<br>**Then** В логах контроллера должна появиться строка "Choice timeout expired."<br>**And** Контроллер должен вернуться в режим ожидания новой карты<br>**And** Транзакция не должна быть сохранена в локальную БД SQLite | |
| **TC-HW-RPI-07** | P1 | `[ ]` | **Scenario:** Контроллер блокирует наливы, если не может проверить статус "Emergency Stop" (Fail-Safe)<br>**Given** RPi-контроллер запущен и готов к работе<br>**And** Флаг "Emergency Stop" на сервере ВЫКЛЮЧЕН ('false')<br>**When** На RPi выполняется команда `sudo iptables -A OUTPUT -p tcp --dport 8000 -j DROP`<br>**And** Пользователь подносит валидную RFID-карту<br>**Then** В логах контроллера должна появиться ошибка сети при проверке статуса<br>**And** Контроллер должен заблокировать операцию налива<br>**And** Новая запись в таблице 'pours' не должна быть создана<br>**When** На RPi выполняется команда `sudo iptables -F` для восстановления связи<br>**And** Пользователь снова подносит карту<br>**Then** Контроллер должен успешно проверить статус и предложить начать налив | |

### **Модуль 2: NFC Handler (Tauri/Rust)**

| ID | Приоритет | Статус | Сценарий (Gherkin) | Комментарий / Bug ID |
| :--- | :--- | :--- | :--- | :--- |
| **TC-HW-NFC-01** | P1 | `[ ]` | **Scenario:** Успешное считывание UID карты<br>**Given** Admin App запущен<br>**And** USB NFC-считыватель ACR122U подключен<br>**And** Пользователь открывает модальное окно "Привязка карты"<br>**Then** UI должен отображать статус "Ожидание карты"<br>**When** Пользователь подносит RFID-карту к считывателю<br>**Then** Модальное окно должно мгновенно изменить статус на "Карта успешно считана!"<br>**And** В окне должен отобразиться корректный UID<br>**And** В течение 1.5 секунд окно должно автоматически закрыться | |
| **TC-HW-NFC-02** | P2 | `[ ]` | **Scenario:** Корректное определение состояния "карта убрана"<br>**Given** Admin App запущен и считыватель подключен<br>**And** Пользователь подносит карту к считывателю<br>**Then** В системных логах должно появиться событие "card-status-changed" с валидным UID<br>**When** Пользователь убирает карту от считывателя<br>**Then** В системных логах должно появиться новое событие "card-status-changed" с payload'ом `{ uid: null, error: null }` | |
| **TC-HW-NFC-03** | P1 | `[ ]` | **Scenario:** Отключение и повторное подключение USB-считывателя<br>**Given** Admin App запущен, и виджет "NFC Reader Status" показывает "Connected"<br>**When** Пользователь физически отключает USB-кабель считывателя<br>**Then** В течение 2-3 секунд виджет "NFC Reader Status" должен изменить статус на "Error" с сообщением "Считыватель не найден."<br>**When** Пользователь снова подключает USB-считыватель<br>**Then** В течение нескольких секунд виджет должен автоматически вернуться в состояние "Connected" | |
| **TC-HW-NFC-04** | P2 | `[ ]` | **Scenario:** Открытие модального окна при отключенном считывателе<br>**Given** Admin App запущен, но USB-считыватель физически отключен<br>**When** Пользователь открывает модальное окно "Привязка карты"<br>**Then** Модальное окно должно немедленно отобразить статус "Ошибка" с сообщением "Считыватель не найден." | |
| **TC-HW-NFC-05** | P3 | `[ ]` | **Scenario:** Стресс-тест: быстрое и многократное прикладывание карты<br>**Given** Admin App запущен, и модальное окно "Привязка карты" открыто<br>**When** Пользователь очень быстро (несколько раз в секунду) прикладывает и убирает карту<br>**Then** Приложение не должно "зависнуть" или аварийно завершиться<br>**And** UI должен в конечном итоге корректно отобразить либо "Успех", либо "Ожидание" | |
| **TC-HW-NFC-06** | P1 | `[ ]` | **Scenario:** Сервер отклоняет привязку карты, которая уже занята<br>**Given** Admin App запущен, считыватель подключен<br>**And** Пользователь открывает модальное окно "Привязка карты"<br>**And** Используется RFID-карта, уже привязанная к другому гостю<br>**When** Пользователь подносит эту карту к считывателю<br>**Then** Модальное окно на короткое время показывает "Карта успешно считана!"<br>**And** Сразу после этого статус меняется на "Ошибка"<br>**And** В окне отображается сообщение от сервера (e.g., "Card with this UID is already assigned...")<br>**And** Модальное окно НЕ должно автоматически закрываться | |

---

## **Раздел 3: Итоговая сводка (Summary)**

### **Статистика выполнения**

| Параметр | Значение |
| :--- | :--- |
| **Всего тест-кейсов** | 13 |
| **Пройдено (Passed)** | |
| **Провалено (Failed)** | |
| **Заблокировано (Blocked)** | |
| **% Выполнения** | |

### **Ключевые выводы и рекомендации**

*   *(Заполняется QA Lead по итогам выполнения протокола)*
*   ...
*   ...

**Итоговое заключение:** *(Заполняется QA Lead)*