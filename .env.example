# .env.example - Пример конфигурационного файла.
# СКОПИРУЙТЕ этот файл в .env и заполните своими значениями.
# Файл .env НИКОГДА не должен попадать в Git.

# --- Настройки для базы данных PostgreSQL ---
# Эти переменные используются сервисом 'postgres' в docker-compose.yml

# Имя пользователя для подключения к БД
POSTGRES_USER=beer_user

# Пароль для пользователя БД.
# ВАЖНО: Замените 'changeme' на сложный и уникальный пароль в вашем настоящем .env файле!
POSTGRES_PASSWORD=changeme

# Название базы данных, которая будет создана
POSTGRES_DB=beer_db

# --- Настройки для Backend API ---
# Эта переменная используется приложением FastAPI для подключения к БД.
# Она автоматически собирается из значений выше.

# Строка для SQLAlchemy (для работы в Docker):
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# Для локальной разработки (если запускаете backend вне Docker):
#DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}